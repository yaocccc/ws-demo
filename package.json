{
  "name": "ws-demo",
  "version": "1.0.0",
  "description": "一个nodejs的websocket服务端和客户端的实现demo",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc --downlevelIteration --skipLibCheck --module commonjs --target es6 --outDir dist",
    "start-server": "ts-node src/wsserver.ts",
    "start-client": "ts-node src/wsclient.ts"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.7.8",
    "@types/ws": "^8.5.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4",
    "ws": "^8.8.1"
  }
}
